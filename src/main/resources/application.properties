spring.datasource.url=jdbc:h2:mem:mytestdb
#logging.level.root=DEBUG
spring.h2.console.enabled=true
# added below so that database is present before execution of data sql
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.port=8081

logging.level.org.springframework.type=trace

logging.level.org.hibernate.stat=DEBUG

## second level cache
# 1. enable second level cache
#spring.jpa.properties.hibernate.cache.use_second_level_cache=true
# 2. which framework to use
#spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.ehcache.internal.EhcacheRegionFactory
# 3. what to cache only cache what is asked
#spring.jpa.properties.jakarta.persistence.sharedCache.mode=ENABLE_SELECTIVE
# 4. what data to cache

## loggin level enabling for ehcache
#logging.level.net.sf.ehcache=debug
```